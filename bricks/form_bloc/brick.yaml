name: form_bloc
description: Create a form bloc package with validation and submission logic using form_bloc packages.

# The following defines the brick repository url.
# Uncomment and update the following line before publishing the brick.
# repository: https://github.com/my_org/my_repo

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.0+1

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ^0.1.1

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, array, or list)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)
#  * an optional separator (list only)
vars:
  name:
    type: string
    description: Your form bloc name
    default: login
    prompt: What is your form bloc's name?
  output_directory:
    type: string
    description: Output directory for the form bloc
    default: app_bloc
    prompt: Where should the form bloc be created?
  has_submission:
    type: boolean
    description: Include form submission logic
    default: true
    prompt: Include form submission logic?
  has_validation:
    type: boolean
    description: Include field validation
    default: true
    prompt: Include field validation?
  field_names:
    type: array
    description: List of field names for the form
    default: ["email", "password"]
    prompt: Enter field names for the form (comma-separated):
    separator: ","