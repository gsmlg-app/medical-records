name: app_utils
description: Utility classes for the medical records app
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: '^3.9.2'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # No assets needed for this package